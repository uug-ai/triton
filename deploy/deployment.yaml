apiVersion: apps/v1
kind: Deployment
metadata:
  name: triton
  namespace: triton
  labels:
    app: triton
    app.kubernetes.io/name: triton
    app.kubernetes.io/component: inference-server
    app.kubernetes.io/part-of: kerberos-hub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: triton
  template:
    metadata:
      labels:
        app: triton
    spec:
      # GPU scheduling: pin Triton to GPU nodes. Uncomment and adjust to match
      # the node labels/taints created by the NVIDIA GPU Operator / device
      # plugin on your cluster. Use MIG or time-slicing to share one physical
      # GPU across models.
      # nodeSelector:
      #   nvidia.com/gpu.present: "true"
      # tolerations:
      #   - key: nvidia.com/gpu
      #     operator: Exists
      #     effect: NoSchedule
      # runtimeClassName: nvidia
      initContainers:
        # Populate the model repository from the versioned models image, then exit.
        # The image bundles model-repository/ (config.pbtxt + exported ONNX
        # weights); bump its tag (deploy/kustomization.yaml `images:`) to roll out
        # new models. Each replica copies into its OWN emptyDir, so no shared/RWX
        # PVC is needed and scaling just works.
        - name: load-models
          image: ghcr.io/uug-ai/triton-models:latest
          command: ["sh", "-c", "cp -a /model-repository/. /models/"]
          volumeMounts:
            - name: model-repository
              mountPath: /models
      containers:
        - name: triton
          # Pin to a Triton version you have tested against your models/drivers.
          image: nvcr.io/nvidia/tritonserver:25.06-py3
          command: ["tritonserver"]
          args:
            - --model-repository=/models
            # `poll` hot-reloads models dropped into the repository without a
            # restart. Use `none` to eager-load once at boot, or `explicit` to
            # load/unload via the model-management API.
            - --model-control-mode=poll
            - --repository-poll-secs=60
            - --log-verbose=0
            # Models are baked into the models image and copied into /models by the
            # load-models initContainer above. To instead serve a model repository
            # straight from S3/MinIO, drop the initContainer + emptyDir and point
            # here (plus the envFrom Secret below):
            # - --model-repository=s3://https://minio.minio-tenant:9000/triton/model-repository
          ports:
            - name: http
              containerPort: 8000
            - name: grpc
              containerPort: 8001
            - name: metrics
              containerPort: 8002
          resources:
            requests:
              cpu: "2"
              memory: 4Gi
            limits:
              nvidia.com/gpu: 1
              cpu: "8"
              memory: 8Gi
          # Model loading (especially TensorRT) can be slow; the startup probe
          # gives Triton time to become ready before liveness starts enforcing.
          startupProbe:
            httpGet:
              path: /v2/health/ready
              port: http
            failureThreshold: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /v2/health/ready
              port: http
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /v2/health/live
              port: http
            periodSeconds: 10
          volumeMounts:
            - name: model-repository
              mountPath: /models
              readOnly: true
            # Triton uses shared memory for CUDA IPC; the container default of
            # 64Mi is too small and causes failures under load.
            - name: dshm
              mountPath: /dev/shm
          # When loading models from S3/MinIO, supply credentials via a Secret:
          # envFrom:
          #   - secretRef:
          #       name: triton-s3-credentials
      volumes:
        # Ephemeral per-pod copy of the model repository, filled by the
        # load-models initContainer from the models image on every start. No PVC.
        - name: model-repository
          emptyDir:
            sizeLimit: 2Gi
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 2Gi
